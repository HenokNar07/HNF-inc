pandas>=2.0
numpy>=1.24
scipy>=1.10
yfinance>=0.2.40
requests>=2.31
pydantic>=2.0
python-dotenv>=1.0
pytest>=7.4
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
slowapi>=0.1.9

# Editable install of math_engine itself, so `import math_engine` works from
# backend/ without a separate build step -- lets hosts like Railway deploy
# straight from `pip install -r requirements.txt` with no custom build command.
-e .
